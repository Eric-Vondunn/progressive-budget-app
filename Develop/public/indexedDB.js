//nothing to pack so nothing to require

function saveRecord(data) {
  console.log(data);

  // register service worker. can register inside or outside of script

  //check for service worker

  //
}
